import { MarkVisitedDirective } from './mark-visited.directive';

describe('MarkVisitedDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkVisitedDirective();
    expect(directive).toBeTruthy();
  });
});
